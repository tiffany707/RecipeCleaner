{
    "scripts": {
    "install-all": "npm install && cd RecipeCleaner/server && npm install && cd ../client && npm install",
    "dev": "cd RecipeCleaner && concurrently \"cd server && node server.js\" \"cd client && npm run dev\""
    },

    "dependencies": {
        "concurrently": "^9.2.1"
    }
}
